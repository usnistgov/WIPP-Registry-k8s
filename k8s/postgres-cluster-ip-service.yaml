apiVersion: v1
kind: Service
metadata:
  name: wipp-registry-postgres-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: wipp-registry-postgres
  ports:
    - port: 5432
      targetPort: 5432
